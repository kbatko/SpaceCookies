using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI : MonoBehaviour {

    public Vector4 players;

    // Use this for initialization
    void Start () {

        //tablica z graczami
        players[0] = 1;
        players[0] = 0;
        players[0] = 0;
        players[0] = 0;
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
